Attribute VB_Name = "README"
' ---------------------------------------------------------
' | Tahapan Membuat Module
' |
' | 1. Tambahkan Function untuk memanggil form setiap Sheet di dalam Module `TampilForm`
' | 2. Tambahkan Alias WorkSheet di dalam Module `WorksheetAlias`
' | 3. Tambahkan Function untuk mendapatkan baris setiap Sheet di dalam Module `GetBaris`
' | 4. Tambahkan Function untuk membuat ID setiap Sheet di dalam Module `BuatId`
' | 5. Tambahkan Function untuk mencari setiap Sheet di dalam Module `Cari`
